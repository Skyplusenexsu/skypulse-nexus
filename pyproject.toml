[project]
name = "skypulse-nexus"
version = "1.0.0"
requires-python = ">=3.10"

dependencies = [
    "fastapi==0.115.6",
    "uvicorn[standard]==0.34.0",
    "pydantic==2.10.5",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
